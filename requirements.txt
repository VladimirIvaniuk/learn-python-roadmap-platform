# Learn Python - Dependencies
# Install: pip install -r requirements.txt

# Junior level - standard library only (no extra deps)

# Web platform (auth, progress)
python-dotenv>=1.0.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# Middle level
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic[email]>=2.5.0
sqlalchemy>=2.0.25
alembic>=1.13.0
psycopg2-binary>=2.9.9
pytest>=7.4.0
pytest-asyncio>=0.23.0
httpx>=0.26.0

# Senior level
redis>=5.0.0
celery>=5.3.0
mypy>=1.8.0
ruff>=0.1.0
black>=24.0.0
