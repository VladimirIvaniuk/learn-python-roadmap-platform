# Завдання — Урок 3: Comprehensions та генератори

## Завдання 1 — List comprehensions
Напиши через comprehension (без циклів for):
1. Список кубів чисел від 1 до 10
2. Список парних чисел від 0 до 50
3. Список слів довше 4 символів з `["cat", "elephant", "dog", "python", "ox"]`
4. Список рядків у форматі `"1 → 1"`, `"2 → 4"`, ..., `"5 → 25"`

---

## Завдання 2 — Dict та Set comprehensions
1. Зі списку `[1, 2, 3, 4, 5]` створи словник `{1: "непарне", 2: "парне", ...}`
2. Інвертуй словник `{"a": 1, "b": 2, "c": 3}` → `{1: "a", 2: "b", 3: "c"}`
3. Зі списку слів `["apple", "Banana", "CHERRY"]` створи множину першних літер у нижньому регістрі

---

## Завдання 3 — Генераторна функція
Напиши генераторну функцію `fibonacci()` що нескінченно генерує числа Фібоначчі.
Виведи перші 15 чисел та суму перших 20 чисел Фібоначчі.

Підказка: `from itertools import islice`

---

## Завдання 4 — Продуктивність
Порівняй час виконання `sum([x**2 for x in range(1_000_000)])` vs
`sum(x**2 for x in range(1_000_000))`.
Виміряй через `import time; t = time.perf_counter()`.
Яка різниця в пам'яті?

---

## Завдання 5 (Challenge) — Pipeline обробки
Маємо список транзакцій:
```python
transactions = [
    {"id": 1, "amount": 150.0, "type": "credit", "user_id": 42},
    {"id": 2, "amount": -50.0, "type": "debit",  "user_id": 7},
    {"id": 3, "amount": 200.0, "type": "credit", "user_id": 42},
    {"id": 4, "amount": -300.0,"type": "debit",  "user_id": 7},
]
```
Через comprehensions (без циклів) знайди:
1. Суму всіх кредитових транзакцій
2. Словник `{user_id: total_amount}` — загальна сума по кожному користувачу
3. Список ID транзакцій де сума від'ємна
